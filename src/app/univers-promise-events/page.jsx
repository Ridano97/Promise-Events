import UniversePage from "../../components/UniversePage";
import { pageMetadata } from "../seo";

export const metadata = pageMetadata({
  title: "Univers Promise Events",
  description:
    "Découvrez l'univers Promise Events : une approche élégante, florale et personnalisée de la décoration événementielle.",
  path: "/univers-promise-events",
  keywords: ["Promise Events", "décoratrice événementielle", "univers décoration événementielle"],
});

export default function UniversPromiseEventsPage() {
  return <UniversePage />;
}
